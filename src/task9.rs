#[test]

/*fn main() {
    let mut sum = 0;
    for i in -3..2 {
        sum += i
    }

    assert!(sum == -3);

    for c in 'a'..='z' {
        println!("{}",c);
    }
}*/


fn main() {
    let mut sum = 0;
    for i in -3..2 {
        sum += i;
    }

    assert!(sum == -5);  // Исправлено: сумма равна -5, а не -3.

    for c in 'a'..='z' {
        println!("{}", c as u8);  // Выводим ASCII значение каждого символа.
    }
}
/*Исправление assert!:
В цикле for i in -3..2 значения i будут -3, -2, -1, 0, 1. Сумма этих чисел равна -5, а не -3, поэтому нужно изменить проверку на правильное значение.

Вывод ASCII значений от 97 до 122 (это символы от 'a' до 'z'):
Сейчас выводятся сами символы, но нужно вывести их ASCII-коды. Для этого можно преобразовать символы в целые числа с помощью as u8.*/